<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--namespace指向mapper.xml所对应的Mapper接口-->
<mapper namespace="cn.edu.tsinghua.tsquality.mapper.IoTDBChunkSeriesStatMapper">
    <update id="createChunkSeriesStatTable" parameterType="String">
       CREATE TABLE IF NOT EXISTS ${chunkSeriesStatTableName}(
            `cid` BIGINT PRIMARY KEY,
            `min_timestamp` BIGINT NOT NULL,
            `max_timestamp` BIGINT NOT NULL,
            `count` BIGINT NOT NULL,
            `miss_count` BIGINT NOT NULL,
            `special_count` BIGINT NOT NULL,
            `late_count` BIGINT,
            `redundancy_count` BIGINT,
            `value_count` BIGINT,
            `variation_count` BIGINT,
            `speed_count` BIGINT,
            `acceleration_count` BIGINT,
            FOREIGN KEY(`cid`) REFERENCES ${chunkTableName}(`cid`) ON DELETE CASCADE
        )
    </update>

    <insert id="insert">
        INSERT IGNORE INTO ${tableName} VALUES (
            #{cid},
            #{stat.minTimestamp},
            #{stat.maxTimestamp},
            #{stat.cnt},
            #{stat.missCnt},
            #{stat.specialCnt},
            #{stat.lateCnt},
            #{stat.redundancyCnt},
            #{stat.valueCnt},
            #{stat.variationCnt},
            #{stat.specialCnt},
            #{stat.accelerationCnt}
        )
    </insert>
</mapper>
