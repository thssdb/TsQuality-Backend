<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--namespace指向mapper.xml所对应的Mapper接口-->
<mapper namespace="cn.edu.tsinghua.tsquality.mapper.IoTDBChunkMapper">
    <update id="createChunkTable" parameterType="String">
        CREATE TABLE IF NOT EXISTS ${chunkTableName}(
            cid INTEGER PRIMARY KEY AUTO_INCREMENT,
            fid INT,
            sid INT,
            offset BIGINT,
            FOREIGN KEY (fid) REFERENCES ${fileTableName}(fid) ON DELETE CASCADE
        )
    </update>

    <insert id="insert" useGeneratedKeys="true" keyProperty="chunk.cid">
        INSERT IGNORE INTO ${tableName} (fid, sid, offset)
        VALUES (#{chunk.fid}, #{chunk.sid}, #{chunk.offset})
    </insert>
</mapper>
