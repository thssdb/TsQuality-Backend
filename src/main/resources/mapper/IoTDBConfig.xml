<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.edu.tsinghua.tsquality.mapper.database.IoTDBConfigMapper">
    <update id="createIoTDBConfigTable">
        CREATE TABLE IF NOT EXISTS iotdb_configs(
            `id` INTEGER PRIMARY KEY AUTO_INCREMENT,
            `port` INT NOT NULL,
            `host` VARCHAR(255) NOT NULL,
            `username` VARCHAR(255) NOT NULL,
            `password` VARCHAR(255) NOT NULL
        )
    </update>

    <update id="create" useGeneratedKeys="true" keyProperty="config.id">
        INSERT INTO iotdb_configs (`port`, `host`, `username`, `password`)
        VALUES (#{config.port}, #{config.host}, #{config.username}, #{config.password})
    </update>

    <update id="update">
        update iotdb_configs
        <set>
            <if test="host != null">
                host = #{host},
            </if>
            <if test="port != 0">
                port = #{port},
            </if>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="password != null">
                password = #{password}
            </if>
        </set>
        <where>
            id = #{id}
        </where>
    </update>
</mapper>